SITE.user = "deploy"
SITE.host = "70.85.31.160"
SITE.remote_dir = "/var/www/apps/christophersaylor.com"

task :default => :build

desc 'deploy the site to the webserver'
task :deploy => [:build, 'deploy:rsync']

# EOF
